const text = `الحمد لله رب العالمين، والصلاة والسلام على أشرف الأنبياء والمرسلين، نبينا محمد وعلى آله وصحبه أجمعين، أما بعد:

فإن العلم بأركان الإيمان من أوجب الواجبات على كل مسلم ومسلمة، إذ لا يصح إيمان المرء حتى يؤمن بها جميعها إيماناً صادقاً يقينياً راسخاً في قلبه.

**أولاً: الإيمان بالله تعالى**

وهو أساس الأركان كلها وأعظمها، ويشمل أربعة أمور:
- الإيمان بوجود الله تعالى وهو أمر فطري جبل الله عليه الخلق
- الإيمان بربوبيته سبحانه، وهو الاعتقاد بأنه الخالق الرازق المدبر المحيي المميت وحده لا شريك له
- الإيمان بألوهيته، وهو إفراده بالعبادة والتأله والمحبة والخوف والرجاء والتوكل
- الإيمان بأسمائه وصفاته، وهو الإيمان بما ثبت له في كتابه أو على لسان نبيه صلى الله عليه وسلم من الأسماء الحسنى والصفات العلى، من غير تحريف ولا تعطيل ولا تكييف ولا تمثيل

قال تعالى: ﴿اللَّهُ لَا إِلَٰهَ إِلَّا هُوَ الْحَيُّ الْقَيُّومُ﴾ [البقرة: 255]`;

function markdownToHtml(md) {
  let html = md;
  html = html.replace(/\*\*(.*?)\*\*/g, '<strong>$1</strong>');
  
  const paragraphs = html.split(/\n\n+/);
  
  const formatted = paragraphs.map(p => {
    if (p.includes('\n- ')) {
      const parts = p.split('\n- ');
      const intro = parts[0];
      const items = parts.slice(1).map(i => `<li>${i.trim()}</li>`).join('');
      return `<p>${intro}</p>\n<ul>\n${items}\n</ul>`;
    }
    if (p.startsWith('- ')) {
       const items = p.split('\n').filter(l => l.startsWith('- ')).map(l => `<li>${l.substring(2).trim()}</li>`).join('');
       return `<ul>\n${items}\n</ul>`;
    }
    return `<p>${p}</p>`;
  });
  
  return formatted.join('\n\n');
}

console.log(markdownToHtml(text));
